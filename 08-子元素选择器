<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>子元素选择器</title>
		<style type="text/css">
			/*
			 * 为第一个p标签设置背景颜色为黄色
			 * 	:first-child 可以选中第一个子元素
			 * 	:last-child  可以选中最后一个子元素
			 *  :nth-child(数字) 可以选中任意位置的子元素
			 * 		-该选择器后边可以指定一个参数，指定要选中第几个元素  
			 * 		even 表示偶数位置的资源  odd表示技术位置的子元素
			 */
			body > p:first-child
			{
				background-color: yellow;
				
			}
			
			p:last-child
			{
				background-color: greenyellow;
			}
			p:nth-child(3)
			{
				font-size: 20px;
			}
			/*
			 * :first-of-type
			 * :last-of-type
			 * :nth-of-type
			 * 		和:first-child这些非常类似
			 * 		只不过child，实在所有的子元素中排列
			 * 		而type，是在当前类型的子元素中排列
			 */
			p:first-of-type
			{
				background-color: deepskyblue; 吧
			}
			
			
			/*
			 * 为span后的一个p元素设置一个背景颜色为黄色
			 * 后一个兄弟元素选择器
			 * 	作用：可以选中一个元素后紧挨着的指定的兄弟元素
			 * 	语法：前一个 + 后一个
			 */
			span + p{
				font-size: 40px;
			}
			
			/*
			 * 选中后边的所有兄弟元素
			 * 	语法：前一个 ~ 后边所有
			 * 
			 */
			span ~ p
			{
				background-color: yellow;
			}
			
			/*
			 * 为所有的p元素设置一个背景颜色为字体颜色为橙色，除了class值为he的
			 * 否定伪类：
			 * 	作用：可以从已选中的元素中剔删出某些元素
			 * 	语法
			 * 		:not(选择器)
			 */
			p:not(.he)
			{
				color: orange;
			}
			
		</style>
	</head>
	<body>
			<p>我是一个p标签</p>
			
			<p>疑是银河落九天</p>
			<p>疑是银河落九天</p>
			<span>hello</span>
			<p>疑是银河落九天</p>
			<p>疑是银河落九天</p>
			<div>
				<p>飞流直下三千尺</p>
			</div>
			<p class="he">疑是银河落九天</p>
			<p>疑是银河落九天</p>
			
	</body>
</html>
